# Empty = same origin. nginx serves the app and proxies /api/ to gunicorn, so
# relative URLs already land on the right host. Set this only if the API ever
# moves to a separate domain.
VITE_API_BASE_URL=
